export const ITEM_TYPES = ["Servicio", "Producto"].map((item) => ({
  value: item,
  text: item,
}));

export const ITEM_CATEGORIES = [
  "Color",
  "Corte",
  "Peinado",
  "Tratamiento",
  "Depilación",
  "Pestañas",
  "Gabinete",
  "Otros",
].map((item) => ({
  value: item,
  text: item,
}));
